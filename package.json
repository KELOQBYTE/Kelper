{
  "name": "interview-coach-overlay",
  "version": "1.0.0",
  "description": "An always-on-top transparent overlay interview coach desktop app",
  "main": "dist/main.js",
  "scripts": {
    "dev": "npm run compile && electron dist/main.js",
    "build": "npm run compile",
    "start": "npm run compile && electron dist/main.js",
    "compile": "tsc && cp src/preload.js dist/ && cp overlay.* settings.* dist/"
  },
  "devDependencies": {
    "@types/node": "^20.19.17",
    "electron": "^27.3.11",
    "typescript": "^5.9.2"
  },
  "author": "Interview Coach",
  "license": "MIT"
}
